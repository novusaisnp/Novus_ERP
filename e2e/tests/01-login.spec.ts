import { test } from '@playwright/test';

test.describe('F1 - Login & Navegação Base', () => {
  test.fixme('login com credenciais válidas', async () => {
    // Implementação em P16.3
  });

  test.fixme('login com credenciais inválidas exibe erro', async () => {
    // Implementação em P16.3
  });
});
