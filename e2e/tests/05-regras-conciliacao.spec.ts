import { test } from '@playwright/test';

test.describe('F5 - CRUD Regras de Conciliação', () => {
  test.fixme('criar, editar e arquivar regra', async () => {
    // Pendente: seletores estáveis no modal de regras de conciliação.
  });
});
