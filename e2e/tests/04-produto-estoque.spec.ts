import { test } from '@playwright/test';

test.describe('F4 - Produto & Movimentação de Estoque', () => {
  test.fixme('cadastro de produto + entrada + saída', async () => {
    // Pendente: POM ProdutoFormPage + EstoqueMovPage com seletores confirmados.
  });
});
